export const mapDefaultCenter = (
    state = { defaultCenter: { lat: 50.0786592, lng: 14.5223136 } },
    action
) => {
    switch (action.type) {
        default:
            return state;
    }
};
