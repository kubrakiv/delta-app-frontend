export const showOrderNumber = (state) => state.plannerInfo.showOrderNumber;
export const showDriver = (state) => state.plannerInfo.showDriver;
export const showCustomer = (state) => state.plannerInfo.showCustomer;

export const selectPlanner = (state) => state.plannerInfo;
